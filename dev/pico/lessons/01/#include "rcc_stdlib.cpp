#include "rcc_stdlib.h"
using namespace std;
 
void classifyGrades(int grade) {
    if ( (grades <= 100) && (grade >= 90))
    printf ("Student got an A/n")
    if ( (grades <= 100) && (grade >= 90))
    printf ("Student got an A/n")
    if ( (grades <= 100) && (grade >= 90))
    printf ("Student got an A/n")   
    if ( (grades <= 100) && (grade >= 90))
    printf ("Student got an A/n")
    if ( (grades <= 100) && (grade >= 90))
    printf ("Student got an A/n")
} 
